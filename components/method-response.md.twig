{% import "includes/macros.twig" as macros %}

{% if (node.response.type and node.response.type != 'void' and node.response.type != 'mixed') or not node.response.description.empty %}
**Return Values**

{{ node.response.type|route('class:short')|markdown_route(curr_template)|join('|')|raw }}

{% if not node.response.description.empty %}
{{ include('components/description.md.twig', {'node': node.response}) }}
{% endif %}
{% endif %}
