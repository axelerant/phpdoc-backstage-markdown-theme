{% set attributes = attributes(node)|specializedAttributes|sortByVisibility %}

{% if attributes is not empty %}
### Attributes

{% for attribute in attributes %}
{{ include('components/attribute.md.twig') }}
{% endfor %}
{% endif %}
