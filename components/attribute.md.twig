{% import "includes/macros.twig" as macros %}

- **#[{{ attribute.attribute|route('class:short')|markdown_route(curr_template) }}]**

{% for argument in attribute.arguments %}
  - {% if argument.name %}${{ argument.name }}: {% endif %}{{ argument.value }}
{% endfor %}
