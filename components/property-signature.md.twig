{% import "includes/macros.twig" as macros %}

`{{ node.visibility }} {% if node.static %}static {% endif %}{{ not node.type ? "mixed" : macros.markdown_argument_type(node.type) }} ${{ node.name }}{% if node.default is not null %} = {{ node.default is not null ? node.default : '""' }}{% endif %}`
