{% import "includes/macros.twig" as macros %}

`{{ node.visibility }} {% if node.abstract %}abstract {% endif %}{% if node.final %}final {% endif %}{% if node.static %}static {% endif %}{% if node.hasReturnByReference %}&amp; {% endif %}{{ node.name }}(`

{% for argument in node.arguments %}
`{% if argument.default is not null %}[{% endif %}{% if not loop.first %}, {% endif %}{{ macros.markdown_route(curr_template, argument.type)|join('|')|raw }} {% if argument.isVariadic %}...{% endif %}{% if argument.byReference %}&{% endif %}${{ argument.name }}{% if argument.default is not null %} = {{ argument.default }} ]{% endif %}`
{% endfor %}

`) : {{ macros.markdown_route(curr_template, node.response.type)|join('|')|raw }}`
