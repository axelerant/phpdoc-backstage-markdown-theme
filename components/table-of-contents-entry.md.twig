- **[{{ node is property ? '$' }}{{ node.name }}{{ node is method or node is function ? '()' }}]({{ link(node)|markdown_link(curr_template) }})**
  {% if node.deprecated %}(deprecated){% endif %}
  {% if node is constant %} = {{ node.value }}{% endif %}
  {% if node is enumCase and node.value %} = {{ node.value }}{% endif %}
  {% if node is property %} : {{ node.type ? node.type|route('class:short')|markdown_route(curr_template)|join('|')|raw : 'mixed' }}{% endif %}
  {% if node is method or node is function %} : {{ node.response.type|route('class:short')|markdown_route(curr_template)|join('|')|raw }}{% endif %}

{% if node.summary %}
*{{ node.summary }}*
{% endif %}
