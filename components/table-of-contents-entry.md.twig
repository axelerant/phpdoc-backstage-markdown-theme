{% import "includes/macros.twig" as macros %}

- **[{{ node is property ? '$' }}{{ node.name }}{{ node is method or node is function ? '()' }}]({{ macros.markdown_link(curr_template, node) }})**
  {% if node.deprecated %}(deprecated){% endif %}
  {% if node is constant %} = {{ node.value }}{% endif %}
  {% if node is enumCase and node.value %} = {{ node.value }}{% endif %}
  {% if node is property %} : `{{ node.type ? macros.markdown_argument_type(node.type) : 'mixed' }}`{% endif %}
  {% if node is method or node is function %} : `{{ macros.markdown_argument_type(node.response.type) }}`{% endif %}

{% if node.summary %}
*{{ node.summary }}*
{% endif %}
