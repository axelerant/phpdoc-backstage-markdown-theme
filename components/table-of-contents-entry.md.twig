- **[{{ node is property ? '$' }}{{ node.name }}{{ node is method or node is function ? '()' }}]({{ link(node) }})**
  {% if node.deprecated %}(deprecated){% endif %}

  {% if node is constant %}
  - Value: `{{ node.value }}`
  {% endif %}

  {% if node is enumCase %}
  {% if node.value %}
  - Value: `{{ node.value }}`
  {% endif %}
  {% endif %}

  {% if node is property %}
  - Type: `{{ node.type ? node.type|route('class:short')|join('|')|raw : 'mixed' }}`
  {% endif %}

  {% if node is method or node is function %}
  - Return Type: `{{ node.response.type|route('class:short')|join('|')|raw }}`
  {% endif %}

{% if node.summary %}
> {{ node.summary }}
{% endif %}
