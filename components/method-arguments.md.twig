{% import "includes/macros.twig" as macros %}

{% if node.arguments|length > 0 %}
**Parameters**

{% for argument in node.arguments %}
- **${{ argument.name }}**: {{ macros.markdown_argument_type(argument.type) }}
  {% if argument.default is not null %}
  - Default: `{{ argument.default }}`
  {% endif %}
  {{ include('components/description.md.twig', {'node': argument}) }}
{% endfor %}
{% endif %}
