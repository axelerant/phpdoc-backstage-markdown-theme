{% import "includes/macros.twig" as macros %}

## {{ node.name }}

{% if node.parent %}
- **Extends**: `{{ macros.markdown_route(node.parent) }}`
{% endif %}

{% if usesPackages %}
- **In Package**:
  {% for breadcrumb in packages(node) %}
  - [{{ breadcrumb.name }}]({{ macros.markdown_link(breadcrumb) }})
  {% endfor %}
{% endif %}

{% if node.interfaces is not empty %}
- **Implements**:
  {% for interface in node.interfaces %}
  `{{ macros.markdown_route(interface) }}{% if not loop.last %}, {% endif %}`
  {% endfor %}
{% endif %}

{% if node.usedTraits is not empty %}
- **Uses**:
  {% for trait in node.usedTraits %}
  `{{ macros.markdown_route(trait) }}{% if not loop.last %}, {% endif %}`
  {% endfor %}
{% endif %}

---

{% if node.isReadOnly %}
- **Read Only**: Yes
{% endif %}

{% if node.isFinal %}
- **Final**: Yes
{% endif %}

{% if node.isAbstract %}
- **Abstract**: Yes
{% endif %}
