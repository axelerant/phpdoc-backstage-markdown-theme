{% import "includes/macros.twig" as macros %}

`{{ node.visibility }} {% if node.final %}final {% endif %}{{ not node.type ? "mixed" : node.type|route('class:short')|markdown_route(curr_template)|join('|')|raw }} {{ node.name }} = {{ node.value | default('""') }}`
