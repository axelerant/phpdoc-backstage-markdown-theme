{% import "includes/macros.twig" as macros %}

`{{ node.visibility }} {% if node.final %}final {% endif %}{{ not node.type ? "mixed" : macros.markdown_route(node.type)|join('|')|raw }} {{ node.name }} = {{ node.value | default('""') }}`
