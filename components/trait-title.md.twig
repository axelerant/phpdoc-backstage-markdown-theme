{% import "includes/macros.twig" as macros %}

## {{ node.name }}

{% if node.usedTraits is not empty %}
- **Uses**:
  {% for trait in node.usedTraits %}
  `{{ trait|route('trait:short')|markdown_route(curr_template) }}{% if not loop.last %}, {% endif %}`
  {% endfor %}
{% endif %}
