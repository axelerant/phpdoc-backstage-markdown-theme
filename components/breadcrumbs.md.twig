{% set breadcrumbs = usesNamespaces ? breadcrumbs(node) : packages(node) %}

{% for breadcrumb in breadcrumbs %}
[{{ breadcrumb.name }}]({{ link(breadcrumb)|markdown_link(curr_template) }}){% if not loop.last %} | {% endif %}
{% endfor %}
